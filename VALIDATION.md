# 可行性验证结果

## 目标范围

只评估 APP 隐私政策、用户协议/授权书和个人信息清单表格中的粗体、文字颜色、
下划线。字符框和真值来自浏览器 DOM 计算样式，与 OCR 预测相互独立。

## 干净密集截图：本地样式模块

共 3,724 个有真值字符。最新逐文档精确数字保存在
`testdata/dense/evaluation.json`，避免算法调整后说明与报告失配。

## AI Studio 端到端：APP 隐私政策

测试图为 760×3000，1,500 个标注字符。AI Studio 实际调用结果：

- PP-OCR 文本序列相似度：0.967；
- PP-OCR 坐标覆盖率：0.961；
- PP-OCR 样式到 VL Markdown 的字符对齐覆盖率：0.943。

最终 `styled.md`（包含 VL 已识别的标题和下划线）指标：

| 属性 | Precision | Recall | F1 |
| --- | ---: | ---: | ---: |
| 粗体 | 0.886 | 0.787 | 0.833 |
| 下划线 | 0.974 | 0.905 | 0.938 |
| 文字颜色 | 0.917 | 1.000 | 0.957 |

完整结果位于 `demo_output/dense_api_evaluation.json`。

## 噪声结论

`testdata/noisy/evaluation.json` 分别记录缩放、JPEG 二次压缩、模糊/噪点和
低对比度色偏下的结果。低对比度影响较小；缩放和强 JPEG 对粗体与细下划线
影响明显。颜色在干净截图上最稳定，粗体仍是主要风险。

## 判断

架构在目标场景中可行，但不能只依赖 FontDNA。现阶段适合 Demo/证据辅助提取：
高置信样式写入 Markdown，粗体中间结果保留 `null`。若进入产品阶段，应收集
真实 APP 字体、字号、缩放与压缩分布，单独训练中文粗体片段分类器，并按 APP
来源拆分训练集和测试集。
