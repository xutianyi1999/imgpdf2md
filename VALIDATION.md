# 可行性验证结果

## 最新：连续粗体片段输出修复与协议场景回归

最新同输入前后对照见 `testdata/policy/evaluation.json`。此前片段补齐已经接受的
低分字符，会被 Markdown 输出的逐字符置信度门槛再次过滤。现在为通过片段规则
的字符保留最低输出置信度 0.25；没有下调片段候选规则或模型阈值。这个数值是
输出决策分数，不是经过校准的正确概率。

共 42 个输入：已有 6 个密集文档、12 个字体矩阵页面，以及新增 8 类协议各自的
原图、JPEG Q55、0.85 倍缩放。新增文档共 7,997 个真值字符；前后版本共享完全
相同的模型预测。以下均为**本地样式模块、DOM 字符框**的字符级粗体指标：

| 测试组 | Precision 前→后 | Recall 前→后 | F1 前→后 |
| --- | --- | --- | --- |
| 已有密集文档 | 0.940 → 0.912 | 0.684 → 0.762 | 0.792 → 0.830 |
| 已有字体矩阵 | 0.924 → 0.902 | 0.519 → 0.571 | 0.665 → 0.699 |
| 新增协议原图 | 0.984 → 0.978 | 0.699 → 0.761 | 0.817 → 0.856 |
| 新增协议 JPEG Q55 | 0.995 → 0.992 | 0.410 → 0.463 | 0.581 → 0.632 |
| 新增协议缩放 0.85 | 0.789 → 0.805 | 0.430 → 0.485 | 0.556 → 0.606 |

这项修复提高召回，但不能保证精确率不下降：已有边界压力页 Precision 从
0.991 降至 0.920，F1 从 0.843 升至 0.879。新增整段粗体协议 Recall 从
0.823 升至 0.897；12 px 灰底授权说明仍只有 0.322，压缩后只有 0.138。

新增纯普通正文含 944 个字符，原图/JPEG 的粗体误报均为 0；缩放后仍误报
14 字（1.48%），修复前后相同。无正例页面应看误报数，不能将报告中约定为 1
的 Recall 当成识别能力证据。该负例也暴露了其他模块的缺陷：原图下划线误报
42 字、颜色误报 7 字；缩放后下划线误报 301 字。这些模块本轮没有修改，不能
将粗体的改善推广为三种样式全部可靠。

新增内容是虚构协议，覆盖窄屏、跨行重点条款、整段加粗、单字强调、纯普通
正文、SDK 表格、深色组合样式、小字号和衬线字体。浏览器检查 8 页均无横向
溢出。生成脚本与真值独立于预测，仍不能代表所有真实 APP 来源；本轮没有新调
AI Studio API，下文端到端数据是历史结果。

可视化入口：`testdata/policy/review.html`。每页提供输入图片、样式预览、
`styled.md`，以及红色误报/蓝色漏报框。使用 `evaluate_policy.py` 和
`build_policy_review.py` 可重建。本轮回归测试 12 项通过。

以下保留修复前的基线结果，以说明各模块与历史端到端表现。

## 目标范围

只评估 APP 隐私政策、用户协议/授权书和个人信息清单表格中的粗体、文字颜色、
下划线。字符框和真值来自浏览器 DOM 计算样式，与 OCR 预测相互独立。

## 干净密集截图：本地样式模块

共 6,682 个有真值字符，覆盖普通/衬线字体、小字号窄屏、深色模式，以及专门的
行内粗体边界压力页。最新逐文档
精确数字保存在
`testdata/dense/evaluation.json`，避免算法调整后说明与报告失配。

接入 TexTAR 并对深色页面做前景极性归一化后，本地粗体结果为：

| 指标 | 仅 FontDNA + 笔画特征 | TexTAR 融合 |
| --- | ---: | ---: |
| Precision | 0.979 | 0.940 |
| Recall | 0.309 | 0.684 |
| F1 | 0.469 | 0.792 |

两列使用完全相同的 6 类文档和字符真值。连续片段规则允许补齐粗体片段内部的
中低分字符，同时拒绝被标点、行边界隔开的孤立高分。新增边界压力页没有参与
最初的片段阈值选择，其 Precision 为 0.991、Recall 为 0.729、F1 为 0.840。

## 参数化字体与字重矩阵

额外 12 组页面、9,578 个字符不复用上述文档布局，系统改变字体、字号、目标
字重和背景主题。总体粗体 Precision 为 0.924、Recall 为 0.519、F1 为 0.665。

| 分组 | Precision | Recall | F1 |
| --- | ---: | ---: | ---: |
| 无衬线字体 | 0.928 | 0.613 | 0.739 |
| 衬线字体 | 0.915 | 0.388 | 0.545 |
| 12 px | 0.824 | 0.211 | 0.336 |
| 13 px | 0.760 | 0.286 | 0.415 |
| 16 px | 0.997 | 0.818 | 0.898 |
| 20 px | 0.919 | 0.765 | 0.835 |
| 字重 600 | 0.786 | 0.248 | 0.378 |
| 字重 700 | 0.939 | 0.600 | 0.732 |
| 字重 900 | 0.997 | 0.818 | 0.898 |

这组矩阵说明当前瓶颈不是单纯缺少阈值调优：12–13 px、600 字重和小字号衬线
字体的可见笔画差异很弱。现阶段应把它们视为低置信场景，不能用 16 px、700
字重页面上的结果代表全部截图。

## AI Studio 端到端：APP 隐私政策

测试图为 760×3000，1,500 个标注字符。AI Studio 实际调用结果：

- PP-OCR 文本序列相似度：0.967；
- PP-OCR 坐标覆盖率：0.961；
- PP-OCR 样式到 VL Markdown 的字符对齐覆盖率：0.943。

最终 `styled.md`（包含 VL 已识别的标题和下划线）指标：

| 属性 | Precision | Recall | F1 |
| --- | ---: | ---: | ---: |
| 粗体 | 0.809 | 0.831 | 0.820 |
| 下划线 | 0.974 | 0.905 | 0.938 |
| 文字颜色 | 0.965 | 1.000 | 0.982 |

这组端到端配置明确偏向召回：相较接入片段补齐前，粗体 Recall 从 0.815 提升到
0.831，Precision 从 0.858 降至 0.809。真实 OCR 框存在边界偏差，因此收益小于
DOM 精确框测试；这也是当前方案需要继续用真实 APP 样本校准的主要部分。

完整结果位于 `demo_output/dense_api_evaluation.json`。

## 噪声结论

`testdata/noisy/evaluation.json` 分别记录两档缩放、两档 JPEG、模糊噪点和
低对比度色偏下的结果。低对比度色偏下粗体 F1 为 0.673；中度 JPEG 为
0.649，强 JPEG 为 0.310。缩放至 0.85/0.67 后粗体 F1 分别降至
0.274/0.095，模糊噪声下为 0.163。这说明已经
被缩小或二次压缩的小字号截图缺少可恢复的笔画信息，应尽量使用原始截图，
不能靠继续调阈值补回。颜色在干净截图和低对比度色偏下最稳定。

## 判断

架构在目标场景中可行，且无需境外 API 或通用视觉大模型。现阶段适合
Demo/证据辅助提取：高置信样式写入 Markdown，粗体中间结果保留 `null`。
TexTAR 对字体和截图退化仍存在域差异；若进入产品阶段，应继续用真实 APP
来源建立只读评测集，即使暂不训练模型，也应按来源校准阈值。
