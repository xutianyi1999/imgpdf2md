# 可行性验证结果

## 最新：固定划分验证、跨行补齐与实际显示修复

本轮不再降低全局粗体阈值。保留上一轮短词证据规则，只增加有约束的软换行
补齐：相邻长行字号接近、行距紧、首尾位置像自然换行、行内没有大间距，且
缺失的最多两字已有支持时才补齐。旧 42 输入回放中新增命中 22 字，误报不变；
新 48 输入中新增命中 111 字，误报也未增加。它不增加模型调用次数。

更主要的收益来自输出修复：原来在 HTML 表格里输出 `**文字**`，以及部分中文
标点边界的星号，标准渲染器不会显示为粗体。现在新增粗体统一输出 `<strong>`。
标题、段落、原始 Markdown 结构仍保留，颜色与下划线继续使用 HTML。

**评测口径修正**：最终显示使用
[markdown-it-py 的 CommonMark 渲染](https://markdown-it-py.readthedocs.io/en/latest/using.html)
再解析生成 HTML 的可见文本与样式，同时对真值和输出统一 NFKC 归一化。
之前自行切换星号状态的评测会误计未显示的粗体。下文历史“最终 Markdown”
数值保留作记录，不能当成新版口径的准确率，也不能直接与新分数作差。

### 新增测试集

`testdata/policy_validation/` 含 16 个页面，预先按内容族划分开发集/保留集各
8 页；覆盖跨行重点、整段加粗、稀疏强调、全普通正文、长文本表格、小字灰底、
宋体和深色背景。每页有原图、JPEG Q55、逐行正负 1 px 坐标偏移，共 48 个输入。
所有版本保持同一划分，未根据保留集修改分类阈值。原始文案是虚构的，布局仍
来自浏览器模板，因此不能替代真实 APP 来源的外部验证。

生成器增加了像素可辨识检查：同一句话以 400 与 700 字重渲染后比较 alpha。
当前 AR PL UMing 在 15/16 px 正文下两者完全相同，大字号标题则不同。因此两页
宋体整页单列为“正文源字重不可辨识”对照，不进入可辨识组汇总；没有删除或
改写原始全量分数。可辨识组主要为 Noto Sans，不代表跨字体泛化已经解决。

保留集结果（仅可辨识的 7 页，各版本使用 DOM 框或已知偏移框）：

| 版本 | Recall 前→后 | Precision 前→后 | 完整行内粗体片段 前→后 |
| --- | --- | --- | --- |
| 原图 | 94.02% → 94.35% | 98.43% → 98.44% | 91/143 → 92/143 |
| JPEG Q55 | 63.93% → 64.65% | 99.91% → 99.91% | 43/143 → 47/143 |
| 坐标偏移 | 91.25% → 92.19% | 98.09% → 98.11% | 78/143 → 84/143 |

包含不可辨识对照的全量保留集原图 Recall 为 84.98% → 85.28%，JPEG 为
57.88% → 58.53%，坐标偏移为 82.29% → 83.13%；完整保存在
`holdout_evaluation.json`。`evaluation.json` 同时保留所有逐页结果及可辨识组。
保留集小字 JPEG Recall 仍只有 20.60%，大比例粗体 JPEG 只有 45.15%，不能
把干净截图的高分推广到压缩小字。当前折中是保留稳定收益，不通过全局放宽
阈值把这些弱项的代价转嫁为大量普通正文误报。

### 两页新 AI Studio 端到端验证

真实调用 PP-OCRv6 与 VL，前后共用同一份返回缓存。按新版标准渲染口径重算：

| 页面 | 最终粗体 Recall 前→后 | Precision 前→后 |
| --- | --- | --- |
| 跨行重点条款 | 83.29% → 93.04% | 100% → 100% |
| 长文本 HTML 表格 | 8.97% → 96.55% | 48.15% → 88.05% |

表格大幅提升主要是显示语法修复，不是模型突然变准：本地样式模块原本就命中
140/145 个粗体字符，误报 19 字；修改后这些标签能在 HTML 表格中实际显示。
跨行页面局部识别多命中 1 字，其余提升同样主要来自标点边界的渲染修复。
两页不足以声称全部真实文档能达到该水平；保留集表格曾用于发现通用渲染缺陷，
所以这两页端到端结果也不声称是完全未见过的盲测。

完整对照在 `api_evaluation.json`。新测试也暴露了既有问题：下划线误报较多，
表格的彩色链接虽然在局部识别出来，经过全文阅读顺序对齐后仍会丢失，本轮未
修复。因此这里的平衡结论仅针对粗体，不代表全部样式均已可用。

查看 `testdata/policy_validation/review.html` 的 48 页对照，以及
`demo_output/validation_after/holdout_table/page-0001/preview.html` 的真实 Markdown
渲染预览。19 项测试通过；两项改动均没有训练模型、使用境外推理 API 或新增
本地模型推理次数。未提交 Git。

以下为历史阶段记录。

## 最新：短词上下文与召回优先模式

本轮尝试了同字模板匹配、单字/短词预测直接融合、降低片段阈值，以及条件补齐。
最终启用的是 **单字基线 + 短词证据补齐 + 最多两字的有证据断点补齐**。
启用 `--textar` 时默认为 `--bold-mode recall`，`balanced` 保留本轮之前的基线。

模型的词级输入依据来自 [TexTAR 官方说明](https://github.com/tex-tar/tex-tar)
和 [FontDNA 模型卡](https://huggingface.co/ClosedBrain/FontDNA-V2)。把同一页的
单字与短词组合使用是本项目的适配实验，并非官方承诺的中文准确率。两种视角
复用同一个 TexTAR 模型，不当作统计上相互独立的模型投票。

最终配置：短词分数至少 0.55，且单字分数至少 0.10 或 FontDNA 至少 0.35。
断点补齐要求两端均已加粗，中间最多两字、有模型支持或为指定标点；换行或
明显横向间距会阻断。它不跨段落传播标签。分数仅是决策依据，不是校准概率。

`testdata/policy/recall_evaluation.json` 记录同一批 42 个输入的最终对照：

| 测试组 | Precision 前→后 | Recall 前→后 | F1 前→后 |
| --- | --- | --- | --- |
| 已有密集文档 | 0.912 → 0.853 | 0.762 → 0.836 | 0.830 → 0.844 |
| 字体矩阵 | 0.902 → 0.872 | 0.571 → 0.709 | 0.699 → 0.782 |
| 新增协议原图 | 0.978 → 0.970 | 0.761 → 0.849 | 0.856 → 0.905 |
| 新增协议 JPEG Q55 | 0.992 → 0.980 | 0.463 → 0.709 | 0.632 → 0.823 |
| 新增协议缩放 0.85 | 0.805 → 0.838 | 0.485 → 0.661 | 0.606 → 0.739 |

新增协议原图的行内粗体片段完整命中数从 57/149 提升到 86/149。这个指标严格
要求片段内每个字符都命中，说明字符召回接近 85% 时，仍不能认为所有重点条款
完整恢复。纯普通正文负例的粗体误报保持原图/JPEG 0 字、缩放 14 字不变。

这轮接受了一定精确率损失，已有密集文档下降约 6 个百分点；字体矩阵下降约
3 个百分点。单字强调和小字号衬线依然是弱项。颜色、下划线识别规则没有修改。

未选用方案：同字模板匹配在协议原图仅补回 6 字、增加 4 字误报；直接融合
上下文让已有密集表格 Precision 从 0.983 降到 0.724；进一步降低全局片段阈值
则让已有密集文档 Precision 降到 0.70 或更低。相关对照分别保存在
`glyph_evaluation.json`、`context_evaluation.json` 和 `recall_profiles.json`。

这是参与方案选择的合成集回归，并非独立盲测；使用 DOM 真值框隔离样式模块，
没有新调用 OCR API，也没有训练模型。完整模型重跑与缓存回放在 SDK 表格的
1,344 字上逐字一致。可查看 `testdata/policy/recall_review.html` 对照页面。
最新用户样例输出另存于 `demo_output/recall/包含粗体和蓝色字体/page-0001/styled.md`。
该样例蓝色链接附近仍有局部粗体误报，尚未建立人工字符真值，不能报告准确率。

对已有带真值的 `dense_mobile_policy` 复用完全相同的 AI Studio JSON 缓存重新
运行两种模式，最终 Markdown 的粗体 Precision 从 0.806 到 0.808，Recall 从
0.888 到 0.899，F1 从 0.845 到 0.851，即额外命中 2 字、误报保持 38 字。
其样式模块坐标评测额外命中 7 字，但不是每一处都能传递到最终 Markdown。
报告分别在 `demo_output/balanced/dense_api_evaluation.json` 和
`demo_output/recall/dense_api_evaluation.json`。这只是一页缓存端到端验证，收益
小于 DOM 框测试，不能把合成集的增幅直接当作真实 OCR 流程的增幅。

16 项回归测试通过，包括词框不重复、不跨明显表格间距、断点不跨行、组合样式
不生成相邻的四个星号；锁文件和语法检查通过。未进行 Git 提交。

后面的章节为历史阶段结果，最新数据以上表及 `recall_evaluation.json` 为准。

## 最新：连续粗体片段输出修复与协议场景回归

最新同输入前后对照见 `testdata/policy/evaluation.json`。此前片段补齐已经接受的
低分字符，会被 Markdown 输出的逐字符置信度门槛再次过滤。现在为通过片段规则
的字符保留最低输出置信度 0.25；没有下调片段候选规则或模型阈值。这个数值是
输出决策分数，不是经过校准的正确概率。

共 42 个输入：已有 6 个密集文档、12 个字体矩阵页面，以及新增 8 类协议各自的
原图、JPEG Q55、0.85 倍缩放。新增文档共 7,997 个真值字符；前后版本共享完全
相同的模型预测。以下均为**本地样式模块、DOM 字符框**的字符级粗体指标：

| 测试组 | Precision 前→后 | Recall 前→后 | F1 前→后 |
| --- | --- | --- | --- |
| 已有密集文档 | 0.940 → 0.912 | 0.684 → 0.762 | 0.792 → 0.830 |
| 已有字体矩阵 | 0.924 → 0.902 | 0.519 → 0.571 | 0.665 → 0.699 |
| 新增协议原图 | 0.984 → 0.978 | 0.699 → 0.761 | 0.817 → 0.856 |
| 新增协议 JPEG Q55 | 0.995 → 0.992 | 0.410 → 0.463 | 0.581 → 0.632 |
| 新增协议缩放 0.85 | 0.789 → 0.805 | 0.430 → 0.485 | 0.556 → 0.606 |

这项修复提高召回，但不能保证精确率不下降：已有边界压力页 Precision 从
0.991 降至 0.920，F1 从 0.843 升至 0.879。新增整段粗体协议 Recall 从
0.823 升至 0.897；12 px 灰底授权说明仍只有 0.322，压缩后只有 0.138。

新增纯普通正文含 944 个字符，原图/JPEG 的粗体误报均为 0；缩放后仍误报
14 字（1.48%），修复前后相同。无正例页面应看误报数，不能将报告中约定为 1
的 Recall 当成识别能力证据。该负例也暴露了其他模块的缺陷：原图下划线误报
42 字、颜色误报 7 字；缩放后下划线误报 301 字。这些模块本轮没有修改，不能
将粗体的改善推广为三种样式全部可靠。

新增内容是虚构协议，覆盖窄屏、跨行重点条款、整段加粗、单字强调、纯普通
正文、SDK 表格、深色组合样式、小字号和衬线字体。浏览器检查 8 页均无横向
溢出。生成脚本与真值独立于预测，仍不能代表所有真实 APP 来源；本轮没有新调
AI Studio API，下文端到端数据是历史结果。

可视化入口：`testdata/policy/review.html`。每页提供输入图片、样式预览、
`styled.md`，以及红色误报/蓝色漏报框。使用 `evaluate_policy.py` 和
`build_policy_review.py` 可重建。本轮回归测试 12 项通过。

以下保留修复前的基线结果，以说明各模块与历史端到端表现。

## 目标范围

只评估 APP 隐私政策、用户协议/授权书和个人信息清单表格中的粗体、文字颜色、
下划线。字符框和真值来自浏览器 DOM 计算样式，与 OCR 预测相互独立。

## 干净密集截图：本地样式模块

共 6,682 个有真值字符，覆盖普通/衬线字体、小字号窄屏、深色模式，以及专门的
行内粗体边界压力页。最新逐文档
精确数字保存在
`testdata/dense/evaluation.json`，避免算法调整后说明与报告失配。

接入 TexTAR 并对深色页面做前景极性归一化后，本地粗体结果为：

| 指标 | 仅 FontDNA + 笔画特征 | TexTAR 融合 |
| --- | ---: | ---: |
| Precision | 0.979 | 0.940 |
| Recall | 0.309 | 0.684 |
| F1 | 0.469 | 0.792 |

两列使用完全相同的 6 类文档和字符真值。连续片段规则允许补齐粗体片段内部的
中低分字符，同时拒绝被标点、行边界隔开的孤立高分。新增边界压力页没有参与
最初的片段阈值选择，其 Precision 为 0.991、Recall 为 0.729、F1 为 0.840。

## 参数化字体与字重矩阵

额外 12 组页面、9,578 个字符不复用上述文档布局，系统改变字体、字号、目标
字重和背景主题。总体粗体 Precision 为 0.924、Recall 为 0.519、F1 为 0.665。

| 分组 | Precision | Recall | F1 |
| --- | ---: | ---: | ---: |
| 无衬线字体 | 0.928 | 0.613 | 0.739 |
| 衬线字体 | 0.915 | 0.388 | 0.545 |
| 12 px | 0.824 | 0.211 | 0.336 |
| 13 px | 0.760 | 0.286 | 0.415 |
| 16 px | 0.997 | 0.818 | 0.898 |
| 20 px | 0.919 | 0.765 | 0.835 |
| 字重 600 | 0.786 | 0.248 | 0.378 |
| 字重 700 | 0.939 | 0.600 | 0.732 |
| 字重 900 | 0.997 | 0.818 | 0.898 |

这组矩阵说明当前瓶颈不是单纯缺少阈值调优：12–13 px、600 字重和小字号衬线
字体的可见笔画差异很弱。现阶段应把它们视为低置信场景，不能用 16 px、700
字重页面上的结果代表全部截图。

## AI Studio 端到端：APP 隐私政策

测试图为 760×3000，1,500 个标注字符。AI Studio 实际调用结果：

- PP-OCR 文本序列相似度：0.967；
- PP-OCR 坐标覆盖率：0.961；
- PP-OCR 样式到 VL Markdown 的字符对齐覆盖率：0.943。

最终 `styled.md`（包含 VL 已识别的标题和下划线）指标：

| 属性 | Precision | Recall | F1 |
| --- | ---: | ---: | ---: |
| 粗体 | 0.809 | 0.831 | 0.820 |
| 下划线 | 0.974 | 0.905 | 0.938 |
| 文字颜色 | 0.965 | 1.000 | 0.982 |

这组端到端配置明确偏向召回：相较接入片段补齐前，粗体 Recall 从 0.815 提升到
0.831，Precision 从 0.858 降至 0.809。真实 OCR 框存在边界偏差，因此收益小于
DOM 精确框测试；这也是当前方案需要继续用真实 APP 样本校准的主要部分。

完整结果位于 `demo_output/dense_api_evaluation.json`。

## 噪声结论

`testdata/noisy/evaluation.json` 分别记录两档缩放、两档 JPEG、模糊噪点和
低对比度色偏下的结果。低对比度色偏下粗体 F1 为 0.673；中度 JPEG 为
0.649，强 JPEG 为 0.310。缩放至 0.85/0.67 后粗体 F1 分别降至
0.274/0.095，模糊噪声下为 0.163。这说明已经
被缩小或二次压缩的小字号截图缺少可恢复的笔画信息，应尽量使用原始截图，
不能靠继续调阈值补回。颜色在干净截图和低对比度色偏下最稳定。

## 判断

架构在目标场景中可行，且无需境外 API 或通用视觉大模型。现阶段适合
Demo/证据辅助提取：高置信样式写入 Markdown，粗体中间结果保留 `null`。
TexTAR 对字体和截图退化仍存在域差异；若进入产品阶段，应继续用真实 APP
来源建立只读评测集，即使暂不训练模型，也应按来源校准阈值。
